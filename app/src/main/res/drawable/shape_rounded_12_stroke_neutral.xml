<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="12dp" />
    <solid android:color="@color/pp_chip_amber_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/pp_divider" />
</shape>
